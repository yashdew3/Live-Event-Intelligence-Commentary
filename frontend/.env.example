# Frontend API base URL (no trailing slash). Leave empty to use Vite proxy in dev.
VITE_API_BASE_URL=http://localhost:8000

# WebSocket base URL (ws:// for local, wss:// for production).
VITE_WS_BASE_URL=ws://localhost:8000
